// src/program/types/program-name.type.ts
// ProgramNameValues is no longer needed as names are arbitrary strings.
// export const ProgramNameValues = ['New Stars', 'Little Giants', 'Dancers'] as const;

export type ProgramName = string;
