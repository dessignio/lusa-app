// backend/studio/studio.controller.ts
import { Controller } from '@nestjs/common';

@Controller('studios')
export class StudioController {
  // Endpoints for studio management can be added here in the future
}
